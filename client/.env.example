# Local dev: leave unset to use Vite proxy → http://localhost:5000 (see vite.config.js)
#
# Production (Vercel): set in Project → Settings → Environment Variables.
# Must be your Railway public URL + /api (no trailing slash after /api)
# Example: https://localsports-api.up.railway.app/api
VITE_API_URL=
